import { ClipboardList, Plus, ShieldCheck } from "lucide-react";
import { Link } from "react-router-dom";
import { useEffect,useState } from "react";
import Layout from "../../components/Layout";
import StatCard from "../../components/StatCard";
import type { ServiceRequest, User } from "../../types";
import { apiFetch } from "../../api/client";
export default function Dashboard({user}:{user:User}){const [items,setItems]=useState<ServiceRequest[]>([]);useEffect(()=>{apiFetch<ServiceRequest[]>("/workorders/requests/").then(setItems).catch(()=>setItems([]));},[]);const active=items.filter(x=>!["VERIFIED","CANCELLED"].includes(x.status)).length;const done=items.filter(x=>x.status==="VERIFIED").length;return <Layout user={user}><div className="hero"><div><span className="eyebrow">CUSTOMER WORKSPACE</span><h2>Good day, {user.first_name || user.username}.</h2><p>Request help, track active jobs, and keep a clean service history.</p></div><Link className="primary small" to="/customer/request"><Plus size={16}/> New request</Link></div><div className="stats"><StatCard label="My requests" value={items.length} icon={<ClipboardList size={20}/>} /><StatCard label="Active" value={active} icon={<ClipboardList size={20}/>} /><StatCard label="Verified" value={done} icon={<ShieldCheck size={20}/>} /></div><div className="panel"><div className="panel-head"><div><h3>Recent requests</h3><p>Track the latest work submitted to your service providers.</p></div></div>{items.slice(0,6).map(i=><div className="list-row" key={i.id}><div><strong>#{i.id} · {i.service_name}</strong><span>{i.address}</span></div><span className={`status ${i.status.toLowerCase()}`}>{i.status.replace("_"," ")}</span></div>)}{items.length===0&&<div className="empty">No service requests yet.</div>}</div></Layout>}
